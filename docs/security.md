# Security Configuration

Use environment secrets, BCrypt/Argon2-compatible password storage for real users, HTTPS termination, strict CORS, short-lived JWTs for admin users, opaque QR tokens, masked document numbers, audit logs, and file storage outside SQL Server.
